checkout:
    post:
        - mkdir -p "$HOME/.go_workspace/src/github.com/$CIRCLE_PROJECT_USERNAME"
        - ln -Tsf "$HOME/$CIRCLE_PROJECT_REPONAME" ".go_workspace/src/github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"
dependencies:
    pre:
        - make deps
          #- go vet ./...
test:
    override:
        - make gen
        - make install
